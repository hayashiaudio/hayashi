{
  "name": "hayashi",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "shadcn": "^4.7.0",
    "turbo": "^2.0.0"
  },
  "dependencies": {
    "@clerk/clerk-react": "^5.61.6",
    "@tanstack/react-virtual": "^3.13.24"
  }
}
